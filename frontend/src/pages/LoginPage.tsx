import { FormEvent, useState } from 'react'
import { ArrowLeft, KeyRound, LogIn } from 'lucide-react'
import { Link, useNavigate } from 'react-router-dom'
import { api } from '../services/api'
export function LoginPage() { const nav = useNavigate(); const [email, setEmail] = useState('admin@nerlens.demo'); const [password, setPassword] = useState('Demo@123'); const [error, setError] = useState(''); const [loading, setLoading] = useState(false); const submit = async (e: FormEvent) => {e.preventDefault(); setLoading(true); setError(''); try { const r = await api.post('/api/auth/login', {email,password}); localStorage.setItem('nerlens_token', r.data.access_token); localStorage.setItem('nerlens_user', JSON.stringify(r.data.user)); nav('/dashboard') } catch {setError('Unable to sign in. The provided demo credentials need the seeded backend running.')} finally {setLoading(false)}}; return <div className="login-page"><Link to="/" className="back-link"><ArrowLeft size={16}/> Back to public site</Link><section className="login-card"><div className="login-icon"><KeyRound/></div><span className="eyebrow">SECURE DEMO ACCESS</span><h1>Sign in to NER-LENS</h1><p>Use a seeded role to manage simulations, roads, and alerts.</p><form onSubmit={submit}><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></label><button className="primary-button full" disabled={loading}><LogIn size={16}/>{loading ? 'Signing in…' : 'Sign in'}</button></form>{error && <div className="error-banner">{error}</div>}<div className="demo-credentials"><b>Seeded demo roles</b><span>admin@nerlens.demo · Demo@123</span><span>authority@nerlens.demo · field@nerlens.demo · citizen@nerlens.demo</span></div></section></div> }
